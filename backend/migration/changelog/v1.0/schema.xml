<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ================================================================== -->
    <!-- USERS TABLE                                                        -->
    <!-- ================================================================== -->
    <changeSet id="001-users" author="economic-dashboard">
        <comment>Create users table for authentication</comment>
        
        <createTable tableName="users">
            <column name="id" type="SERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="username" type="VARCHAR(50)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="email" type="VARCHAR(100)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="hashed_password" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="full_name" type="VARCHAR(100)"/>
            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
        
        <createIndex tableName="users" indexName="idx_users_username">
            <column name="username"/>
        </createIndex>
        
        <createIndex tableName="users" indexName="idx_users_email">
            <column name="email"/>
        </createIndex>
        
        <rollback>
            <dropTable tableName="users"/>
        </rollback>
    </changeSet>

    <!-- ================================================================== -->
    <!-- SEED DEFAULT ADMIN USER                                            -->
    <!-- ================================================================== -->
    <changeSet id="002-seed-admin" author="economic-dashboard">
        <comment>Seed default admin user (msadmin/all4one)</comment>
        <sqlFile path="postgres/seed-admin-user.sql" relativeToChangelogFile="true"/>
        <rollback>
            <delete tableName="users">
                <where>username = 'msadmin'</where>
            </delete>
        </rollback>
    </changeSet>
<changeSet id="003-roles" author="economic-dashboard">
    <comment>Create roles table</comment>

    <createTable tableName="roles">
        <column name="id" type="SERIAL">
            <constraints primaryKey="true" nullable="false"/>
        </column>
        <column name="name" type="VARCHAR(50)">
            <constraints nullable="false" unique="true"/>
        </column>
        <column name="description" type="VARCHAR(255)"/>
        <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
            <constraints nullable="false"/>
        </column>
    </createTable>

    <rollback>
        <dropTable tableName="roles"/>
    </rollback>
</changeSet>

<changeSet id="004-seed-roles" author="economic-dashboard">
    <comment>Seed default roles</comment>

    <insert tableName="roles">
        <column name="name" value="admin"/>
        <column name="description" value="Full access to users and APIs"/>
    </insert>

    <insert tableName="roles">
        <column name="name" value="analyst"/>
        <column name="description" value="Can view and work with dashboard data"/>
    </insert>

    <insert tableName="roles">
        <column name="name" value="inactive"/>
        <column name="description" value="No active access"/>
    </insert>
</changeSet>

<changeSet id="004-seed-roles" author="economic-dashboard">
    <comment>Seed default roles</comment>

    <insert tableName="roles">
        <column name="name" value="admin"/>
        <column name="description" value="Full access to users and APIs"/>
    </insert>

    <insert tableName="roles">
        <column name="name" value="analyst"/>
        <column name="description" value="Can view and work with dashboard data"/>
    </insert>

    <insert tableName="roles">
        <column name="name" value="inactive"/>
        <column name="description" value="No active access"/>
    </insert>
</changeSet>

<changeSet id="005-add-role-id-to-users" author="economic-dashboard">
    <comment>Add role_id to users table</comment>

    <addColumn tableName="users">
        <column name="role_id" type="INT"/>
    </addColumn>

    <addForeignKeyConstraint
        baseTableName="users"
        baseColumnNames="role_id"
        referencedTableName="roles"
        referencedColumnNames="id"
        constraintName="fk_users_roles"/>

    <rollback>
        <dropForeignKeyConstraint baseTableName="users" constraintName="fk_users_roles"/>
        <dropColumn tableName="users" columnName="role_id"/>
    </rollback>
</changeSet>

<changeSet id="006-assign-admin-role" author="economic-dashboard">
    <comment>Assign admin role to default admin user</comment>

    <sql>
        UPDATE users
        SET role_id = (SELECT id FROM roles WHERE name = 'admin')
        WHERE username = 'msadmin';
    </sql>
</changeSet>

</databaseChangeLog>
