# Full Stack Docker Compose - Economic Indicators Dashboard
# Run: docker compose up -d

services:
  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    container_name: economic-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: economic_indicators
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - economic-network

  # Liquibase Migration
  liquibase:
    image: liquibase/liquibase:4.25-alpine
    container_name: economic-liquibase
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend/migration/changelog:/liquibase/changelog
    environment:
      - LIQUIBASE_DUPLICATE_FILE_MODE=WARN
      - LIQUIBASE_VALIDATE_XML_CHANGELOG_FILES=false
    command: >
      --url=jdbc:postgresql://db:5432/economic_indicators
      --username=postgres
      --password=postgres
      --changeLogFile=db.changelog-master.xml
      update
    networks:
      - economic-network

  # FastAPI Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: economic-api
    depends_on:
      liquibase:
        condition: service_completed_successfully
    environment:
      MSFINTECH_POSTGRES_SERVER: db
      MSFINTECH_POSTGRES_PORT: 5432
      MSFINTECH_POSTGRES_USER: postgres
      MSFINTECH_POSTGRES_PASSWORD: postgres
      MSFINTECH_POSTGRES_DB: economic_indicators
    ports:
      - "8000:8000"
    networks:
      - economic-network

  # React Frontend
  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    container_name: economic-ui
    depends_on:
      - backend
    ports:
      - "8081:8081"
    networks:
      - economic-network

volumes:
  postgres_data:

networks:
  economic-network:
    driver: bridge
